<Window x:Class="BestAutoClicker.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BestAutoClicker"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="MultiTool" 
        ResizeMode="CanMinimize"
        MinHeight="400" 
        MinWidth="700" 
        MaxHeight="400" 
        MaxWidth="700" 
        Background="{StaticResource HomeWindow}"
        FontFamily="{materialDesign:MaterialDesignFont}" >
    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1.5*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="2*"></ColumnDefinition>
            <ColumnDefinition Width="2*"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="50"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>


        <CheckBox Content="Multiple Points Enable" VerticalAlignment="Center" Foreground="{StaticResource Text}" Background="White"  ></CheckBox>
         
        <Button Grid.Row="0" Grid.Column="1" Content="Record (F5)" Background="{StaticResource Buttons}" BorderBrush="Transparent" ></Button>
       
        <Button Grid.Row="1" Content="Clear Points" FontSize="10" HorizontalAlignment="Left" Background="{StaticResource Buttons}" BorderBrush="Transparent"></Button>
        
        
        <Label Content="Custom Points" Grid.Row="1" Grid.ColumnSpan="2" HorizontalAlignment="Center" Foreground="{StaticResource Text}" BorderBrush="White"></Label>
        <ListBox Background="{StaticResource Chat}" ScrollViewer.CanContentScroll="False" BorderThickness="1" Foreground="{StaticResource Text}" Grid.Row="2" Grid.ColumnSpan="2">

           
            <ListBoxItem>(123,124)</ListBoxItem>
        </ListBox>
    </Grid>
</Window>
